# Use a local cache directory with proper permissions
cache=/Users/apple/.npm-cache
# Avoid permission issues by not using sudo
unsafe-perm=true
# Set the log level to verbose to see more details about any issues
loglevel=verbose
